{
    "api_base_url": {
        "description": "ubi-go服务器地址",
        "type": "string",
        "hint": "数据查询依赖ubi-go,请根据插件github主页自部署ubi-go后端,示例: http://www.example.com:12910",
        "default": "http://127.0.0.1:12910"
    },
    "default_platform": {
        "description": "默认查询平台（当用户未指定时使用）",
        "type": "string",
        "hint": "可选 uplay / xbl / psn",
        "default": "uplay",
        "enum": ["uplay", "xbl", "psn"]
    }
}